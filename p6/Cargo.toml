[package]
name = "p6"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
async-std = { version = "1.10", feautres = ["unstable"] }
sha-1 = "0.10"
base64ct = { version = "1.4", features = ["alloc"] }